plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("kotlin-kapt")
    id("com.google.gms.google-services")
    id("com.chaquo.python") // حقن محرك بايثون
}

android {
    namespace = "com.jamesfirstok.aegis"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.jamesfirstok.aegis"
        minSdk = 26
        targetSdk = 34
        versionCode = 200
        versionName = "2.0.0-Tactical-Operational"

        // ضبط المعالجات لضمان توافق مكتبات الراديو والـ JNI
        ndk {
            abiFilters += listOf("armeabi-v7a", "arm64-v8a", "x86_64")
        }

        // إعدادات محرك بايثون داخل التطبيق
        python {
            version = "3.10"
            pip {
                install("scapy")
                install("numpy")
                install("langchain")
            }
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = false // تعطيل Proguard لتجنب حذف دوال الـ JNI الحساسة
            isShrinkResources = false
        }
    }

    externalNativeBuild {
        cmake {
            path = file("src/main/cpp/CMakeLists.txt")
            version = "3.22.1"
        }
    }
}

dependencies {
    // تم الإبقاء على مكاتبك وإضافة مكاتب التحييد
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.room:room-runtime:2.6.1")
    
    // إضافة مكتبة الاتصالات اللاسلكية المتقدمة
    implementation("commons-net:commons-net:3.10.0") 
    
    // دعم تشغيل بايثون (Chaquopy)
    implementation("com.chaquo.python:gradle:15.0.1")
}
