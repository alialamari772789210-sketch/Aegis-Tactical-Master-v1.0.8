pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    // هذا الوضع يضمن أن جميع أجزاء المشروع تستخدم نفس المستودعات المركزية
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // ضروري جداً لمكتبات الرادار SDR والدرونات
    }
}

rootProject.name = "Aegis-Tactical-Master"
include ':app'
