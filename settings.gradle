pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri("https://chaquo.com/maven") } // أضف هذا السطر هنا
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://chaquo.com/maven") } // أضف هذا السطر هنا أيضاً
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Aegis-Tactical-Master"
include ':app'
